{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the ASCII art converter window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-save"]
}
