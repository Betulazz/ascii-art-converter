Place the bundled FFmpeg binary here before running `tauri build`.

For Windows x64 Tauri expects:

```text
ffmpeg-x86_64-pc-windows-msvc.exe
```

The application also supports `ASCII_ART_FFMPEG` or `ffmpeg` on `PATH` during development.
